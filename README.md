# nota:

este es mi primer programa

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     la basse para multiplicar                   [number] [required]
  -l, --lista    lista la tabla de multiplicar        [boolean] [default: false]
  -h, --hasta    contador para el ciclo for                [number] [default: 1]
```